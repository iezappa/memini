# WSL writes one of these next to every file copied from Windows.
*:Zone.Identifier

# OS junk
.DS_Store
Thumbs.db

# Agent tooling cache — the registry itself is tracked, the cache is not.
.atl/.skill-registry.cache.json

# Working copy of the standard, kept here to be read without leaving the repo.
# The canonical one is the repository below. A copy committed here ages inside
# it and ends up contradicting that one with all the authority of sitting in
# the project.
# https://github.com/iezappa/standardizer_multiplatform
Estandarización/
